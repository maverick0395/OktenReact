const jsonplaceholderBaseUrl = 'https://jsonplaceholder.typicode.com';

export const jsonplaceholderUrls = {
    users: `${jsonplaceholderBaseUrl}/users`,
    posts: `${jsonplaceholderBaseUrl}/posts`,
    comments: `${jsonplaceholderBaseUrl}/comments`
}

export default jsonplaceholderBaseUrl;

